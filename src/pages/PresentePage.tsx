import Presente from '@/src/components/Presente';

/** Ruta `/presente` — Fase 1. Wrapper delgado: el contenido sigue siendo el mismo `Presente.tsx`, sin cambios internos. */
export default function PresentePage() {
  return <Presente />;
}
