import Crear from '@/src/components/Crear';

/** Ruta `/crear` — Fase 1. Wrapper delgado: el contenido sigue siendo el mismo `Crear.tsx`, sin cambios internos. */
export default function CrearPage() {
  return <Crear />;
}
