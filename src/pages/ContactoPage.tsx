import Contacto from '@/src/components/Contacto';

/** Ruta `/contacto` — F7. Wrapper delgado, mismo patrón que el resto de `src/pages/*Page.tsx`. */
export default function ContactoPage() {
  return <Contacto />;
}
