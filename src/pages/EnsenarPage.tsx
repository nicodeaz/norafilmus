import Ensenar from '@/src/components/Ensenar';

/** Ruta `/ensenar` — Fase 1. Wrapper delgado: el contenido sigue siendo el mismo `Ensenar.tsx`, sin cambios internos. */
export default function EnsenarPage() {
  return <Ensenar />;
}
