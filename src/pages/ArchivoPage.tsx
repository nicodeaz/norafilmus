import Archivo from '@/src/components/Archivo';

/** Ruta `/archivo` — F8. Wrapper delgado, mismo patrón que el resto de `src/pages/*Page.tsx`. */
export default function ArchivoPage() {
  return <Archivo />;
}
