import Producir from '@/src/components/Producir';

/** Ruta `/producir` — Fase 1. Wrapper delgado: el contenido sigue siendo el mismo `Producir.tsx`, sin cambios internos. */
export default function ProducirPage() {
  return <Producir />;
}
