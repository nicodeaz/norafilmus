import Trayectoria from '@/src/components/Trayectoria';

/** Ruta `/trayectoria` — Fase 1. Wrapper delgado: el contenido sigue siendo el mismo `Trayectoria.tsx`, sin cambios internos. */
export default function TrayectoriaPage() {
  return <Trayectoria />;
}
